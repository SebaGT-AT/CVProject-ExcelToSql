spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:data_importer_pro}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

application:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
